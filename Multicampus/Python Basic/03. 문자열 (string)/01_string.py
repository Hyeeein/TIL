## 문자열 연결
# 1) 문자열과 문자열 연결 : + 연산자 사용
# 2) 문자열과 숫자 연결 : 숫자를 문자열로 형변환하여 + 연산자로 연결 / 문자열로 형변환 str(숫자)

## 문자열에서 사용되는 연산자 : +, *
# + : 문자열과 문자열을 연결 -> 하나의 문자열로 합치는 연산
# * : 문자열을 곱하는 수만큼 반복생성

string = '파이썬'
result = string * 3
print(result)

## 문자열 인덱싱 (indexing) : 인덱스로 문자의 위치를 나타내는 것
# 인덱스 : 문자의 위치, 0부터 시작함. 마지막 문자는 -1

id = '020201-3030301'  # 주민번호
